# Overview

It's a model - text classifier - that takes as input a poem and as output it classifies it as being by Edgar Allan Poe or Robert Frost. It's trained by using Markov Model trained on corpus by each author.

